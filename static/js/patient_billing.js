document.addEventListener('DOMContentLoaded', function() {
    // Patient billing page specific functionality
    // The billing data is already rendered server-side in the template
    // This file can be used for any additional client-side functionality

    console.log('Patient billing page loaded');
});
